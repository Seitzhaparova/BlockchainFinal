import React from "react";
import ReactDOM from "react-dom/client";
import "./main_page.css"; // ваши стили
import StartPage from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <StartPage />
  </React.StrictMode>
);


